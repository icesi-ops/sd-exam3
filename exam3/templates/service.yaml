apiVersion: v1
kind: Service
metadata:
  name: {{ include "exam3.fullname" . }}
  labels:
    {{- include "exam3.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "exam3.selectorLabels" . | nindent 4 }}
